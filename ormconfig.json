{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "piotrsokolinski",
  "password": "Password1!",
  "database": "project-database",
  "entities": ["dist/**/*.entity.js"],
  "synchronize": true,
  "logging": true,
  "migrations": ["migration/*.js"],
  "cli": {
    "migrationsDir": "migration"
  }
}
